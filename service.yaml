apiVersion: v1
kind: Service
metadata:
  name: my-flask-app-service
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 5000  # Flask typically runs on port 5000 inside the container
  selector:
    app: my-flask-app  # Corrected: remove the hyphen and space
